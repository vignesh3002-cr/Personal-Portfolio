{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist",
        "nodeVersion": "22.x",
        "installCommand": "npm install --unsafe-perm",
        "buildCommand": "npm run vercel-build"
      }
    }
  ]
}